# Example mobile parameters for the replay harness.
# These are ILLUSTRATIVE placeholders, not the values used for the
# energy measurements in the paper. Replace with platform measurements.
lambda 0.5
alpha  0.5
t_hat0 1000
t_be   50
t_max  30000
